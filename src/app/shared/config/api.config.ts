export const SESSION_STORAGE_KEYS = {
  accessToken: 'axiomlms.accessToken',
  refreshToken: 'axiomlms.refreshToken',
  activeRole: 'axiomlms.activeRole',
  username: 'axiomlms.username',
} as const;
